server:
  port: 3377

spring:
  application:
    name: cloud-alibaba-config
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        # dataId 前缀
        prefix: application
        # dataId 后缀
        fileExtension: yml
